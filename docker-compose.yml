version: "3.9"
services:
  tradesentinel:
    build: .
    command: python orchestrator.py --dashboard
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
